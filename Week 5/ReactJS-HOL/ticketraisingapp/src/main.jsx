import React,{useState}from'react';import{createRoot}from'react-dom/client';import'./styles.css';
function ComplaintRegister(){const[name,setName]=useState(''),[complaint,setComplaint]=useState('');function handleSubmit(e){e.preventDefault();if(!name.trim()||!complaint.trim())return alert('Please enter employee name and complaint.');const ref=`TKT-${Date.now().toString().slice(-6)}`;alert(`Complaint registered for ${name}. Reference number: ${ref}`);setName('');setComplaint('')}return <main><h1>Raise a Complaint</h1><form onSubmit={handleSubmit}><label>Employee Name<input value={name} onChange={e=>setName(e.target.value)} /></label><label>Complaint<textarea value={complaint} onChange={e=>setComplaint(e.target.value)} /></label><button>Submit Complaint</button></form></main>}createRoot(document.getElementById('root')).render(<ComplaintRegister/>);
